export interface User {
  id: number;
  name: string;
  lastName: string;
  userName: string;
  phoneNumber: string;
}

export interface UserById {
  id: number;
  name: string;
  lastName: string;
  userName: string;
  phoneNumber: string;
}
